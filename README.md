# knife cookbook site acceptance tests

Acceptance tests written against the output of `knife cookbook site` commands.

## Setup

1. Copy a valid Hosted Chef PEM to `./chef/default.pem`
2. bundle exec rspec
